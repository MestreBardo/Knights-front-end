import { ModificadoresPipe } from './modificadores.pipe';

describe('ModificadoresPipe', () => {
  it('create an instance', () => {
    const pipe = new ModificadoresPipe();
    expect(pipe).toBeTruthy();
  });
});
